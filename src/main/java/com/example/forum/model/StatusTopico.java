package com.example.forum.model;

// defino a regra para que o usuario "digite o que eu quero" para que eu possa retornar a respostar
// ou retornar como erro
public enum StatusTopico {

	NAO_RESPONDIDO,
	NAO_SOLUCIONADO,
	SOLUCIONADO,
	FECHADO;

}
